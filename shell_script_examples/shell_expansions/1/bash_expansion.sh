#!/bin/bash 
#how to store a value in a variable
counter=2 
#when we use double quotes variable substitution takes place
echo "$counter"
#when we use a single quote the interpreter treats the data as a string.
echo '$counter'
